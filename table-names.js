const map = {};

const names = Object.keys(map);

const collectionsMap = {};

const collectionsNames = Object.keys(collectionsMap);

const singleToCollection = {};

module.exports.map = map;
module.exports.names = names;
module.exports.collectionsMap = collectionsMap;
module.exports.collectionsNames = collectionsNames;
module.exports.singleToCollection = singleToCollection;
